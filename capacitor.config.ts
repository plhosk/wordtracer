import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wordtracer.app',
  appName: 'Word Tracer',
  webDir: 'dist',
  backgroundColor: '#0d1420',
};

export default config;
